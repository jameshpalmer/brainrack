{
  "name": "replicache-byob-starter",
  "version": "0.0.0",
  "devDependencies": {
    "typescript": "5.6.3"
  },
  "scripts": {
    "build": "pnpm run build -ws --if-present"
  },
  "type": "module",
  "engines": {
    "node": ">=16.15.0",
    "pnpm": ">=9.9.0"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "dependencies": {
    "@rocicorp/rails": "0.11.1",
    "replicache": "^15.0.1"
  }
}
